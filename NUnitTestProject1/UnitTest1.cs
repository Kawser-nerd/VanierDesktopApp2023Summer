using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;

namespace NUnitTestProject1
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        // This is the declaration of the Test Method or program Space. You
        // need to define it to say that the following method will work as a Test Method
        public void TestAdditionMethod()
        {
            /*
             * Each of the test method has three steps... all of them together call "AAA"
             * 
             * A~ Arrange
             * A~ Act
             * A~ Assert
             */

            //Arrage: In the arrange, we need to call the classes which are going to
            // be tested in this test class

            NU
        }
    }
}
